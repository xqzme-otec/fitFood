exclude = [
    '^http://localhost',
    '^http://127.0.0.1',
    '^https://gitlab\.pg\.innopolis\.university',
    # Hosted docs site (GitHub Pages): only resolves after Pages is enabled and
    # the `docs` workflow deploys from main; skip the check until then.
    '^https://xqzme-otec\.github\.io',
    # Course reference docs that live outside this repo (linked from Assignment_*.md).
    'Repository_Requirements\.md',
    'Artifact_Requirements\.md'
]
# Skip links to private/internal network addresses (e.g. the self-hosted
# deploy at http://10.93.26.202:8000/) — they are unreachable from CI runners.
exclude_private = true
exclude_mail = true
