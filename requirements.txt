# --- Web / API ---
fastapi==0.136.3
uvicorn==0.49.0
python-multipart==0.0.32

# --- БД / ORM ---
SQLAlchemy==2.0.50
# Драйвер PostgreSQL (используется в Docker / проде; для SQLite не нужен).
psycopg2-binary==2.9.10

# --- Валидация / конфиг ---
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0
python-dotenv==1.2.2

# --- Безопасность / JWT ---
bcrypt==5.0.0
python-jose==3.5.0

# --- ML-классификатор категорий (xgb_model.pkl) ---
joblib==1.5.3
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.2.0
pandas==3.0.3

# --- OCR (опционально, для реального распознавания чеков) ---
# pytesseract==0.3.13
# Pillow==11.0.0
